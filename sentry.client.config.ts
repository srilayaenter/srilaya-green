// Sentry client config — only active when NEXT_PUBLIC_SENTRY_DSN env var is set.
// Install @sentry/nextjs and set the env var to enable error tracking.
// This file is intentionally empty until then to avoid build-time errors.
export {};
